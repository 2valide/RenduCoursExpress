<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= titre %></title>
  </head>
  <body>
    <header>
      <img src="/logo.png" alt="Logo Université" />
      <nav>
        <ul>
          <li><a href="/">Accueil</a></li>
          <li><a href="/cours">Cours</a></li>
        </ul>
      </nav>
    </header>
    <section>
      <h1><%= titre %></h1>
      <p><%= descriptif %></p>
    </section>
    <section>
      <h2>Enseignants</h2>
      <ul>
        <% enseignants.forEach(function(enseignant) { %>
        <li><%= enseignant %></li>
        <% }); %>
      </ul>
    </section>
    <footer>
      <p>Contact : <%= enseignants[0] %></p>
    </footer>
  </body>
</html>
